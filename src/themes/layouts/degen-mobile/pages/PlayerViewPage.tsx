import React from 'react'
import { PlayerView as OriginalPlayerView } from '../../../../components'

// Re-export the original PlayerView with degen-mobile theme context
export default function PlayerViewPage() {
  return <OriginalPlayerView />
}