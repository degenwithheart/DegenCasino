import React from 'react'
import { Transaction as OriginalTransaction } from '../../../../components'

// Re-export the original Transaction component with degen-mobile theme context
export default function TransactionPage() {
  return <OriginalTransaction />
}