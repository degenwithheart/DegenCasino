// Roulette Slice bet array: multipliers for each slice (example: 8 slices)
// Adjust multipliers and slice count as desired
export const betArray = [14, 7, 5, 3, 2, 1.5, 1.2, 0]; // Example: 1 big win, several smaller, some lose
